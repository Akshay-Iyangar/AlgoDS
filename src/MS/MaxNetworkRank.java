package MS;


/**
 * An infrastructure consisting of N cities, numbered from 1 to N,
 * and M bi direction roads between them is given. Roads do not
 * intersect apart from at their start and end points (they can
 * pass through underground tunnels to avoid collisions).
 *
 * For each pair of cities directly connected by a road, let's define
 * their networks rank as the total number of roads that are connected
 * to either of the two cities.
 *
 *
 *two arrays are given A,B consisting of  M integers each and a
 *
 *
 */
public class MaxNetworkRank {
}
